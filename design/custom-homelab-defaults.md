# Defaults value for a homelab setting

The `custom-homelab-defaults.yml` template is specifically designed for **a homelab setting**. It provides overrides to allow the templates to on how some default values can be overridden.
